services:
  postgres:
    image: postgres:16-alpine
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: flat_finder
      POSTGRES_USER: flat_finder
      POSTGRES_PASSWORD: flat_finder_dev
    volumes:
      - pgdata:/var/lib/postgresql/data

volumes:
  pgdata:
